apiVersion: v1
kind: Service
metadata:
  name: nginx-service
  namespace: demo
spec:
  type: {{ .Values.service.type }}
  selector:
    app: nginx-demo
  ports:
    - protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}